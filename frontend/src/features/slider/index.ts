// Components
export { HeroSlider, SliderControls } from './components';

// Hooks
export { useSlider } from './hooks/useSlider';

// Types
export type { 
  SliderItem, 
  HeroSliderProps, 
  SliderControlsProps, 
  UseSliderOptions, 
  UseSliderReturn 
} from './types';

// Data
export { heroSliderData, sliderSettings } from './data/sliderData';