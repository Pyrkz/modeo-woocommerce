'use client';

import { AboutFeaturesProps } from '../types';

export function AboutFeatures(_props: AboutFeaturesProps) {
  void _props; // ESLint: intentionally unused
  // Features are now fully integrated into AboutHero component as inline elements
  // This component returns null to avoid any spacing issues
  return null;
}